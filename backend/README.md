Placeholder readme for the backend.

This does require poetry. Ensure you run

```
pip install poetry
poetry install
```

# Testing
Within the backend directory run tests with

```
poetry run coverage run -m pytest  
poetry run coverage report
```